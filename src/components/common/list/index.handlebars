<div class="components-common-list">
    <h6>{{title}}</h6>
    {{#each items}}
        <a
                class="link components-common-list__item"
                href="{{url}}"
        >{{name}}</a>
    {{/each}}
</div>
